# 设备发现与管理

## 🔍 设备发现架构

Ollama的设备发现模块负责**自动检测**和**智能管理**系统中的计算设备，为模型推理提供最优的硬件资源配置。

```mermaid
graph TB
    subgraph "发现入口"
        DiscoverEntry[设备发现入口<br/>discover.GPUDevices()]
        Bootstrap[引导程序<br/>bootstrapDevices()]
        RunnerProbe[运行器探测<br/>GetDevicesFromRunner()]
    end
    
    subgraph "设备检测"
        GPUDetect[GPU检测器<br/>枚举显卡设备]
        CPUDetect[CPU检测器<br/>处理器信息]
        MemoryProbe[内存探测<br/>VRAM/RAM查询]
    end
    
    subgraph "硬件抽象"
        CUDA[CUDA设备<br/>NVIDIA显卡]
        ROCm[ROCm设备<br/>AMD显卡]
        Metal[Metal设备<br/>Apple GPU]
        Vulkan[Vulkan设备<br/>通用GPU API]
        OpenCL[OpenCL设备<br/>异构计算]
    end
    
    subgraph "设备信息"
        DeviceInfo[设备信息结构<br/>ml.DeviceInfo] 
        GpuInfo[GPU信息<br/>discover.GpuInfo]
        Capabilities[能力检测<br/>性能特征]
    end
    
    DiscoverEntry --> GPUDetect
    Bootstrap --> CPUDetect
    RunnerProbe --> MemoryProbe
    
    GPUDetect --> CUDA
    GPUDetect --> ROCm
    GPUDetect --> Metal
    GPUDetect --> Vulkan
    GPUDetect --> OpenCL
    
    CUDA --> DeviceInfo
    ROCm --> DeviceInfo
    Metal --> GpuInfo
    Vulkan --> Capabilities
    OpenCL --> Capabilities
```

## 🚀 发现流程详解

### 设备发现时序图
```mermaid
sequenceDiagram
    participant Main as 主进程
    participant Discover as 发现模块
    participant Runner as Runner进程
    participant GPU as GPU库
    participant Device as 物理设备
    
    Main->>Discover: GPUDevices(ctx, [])
    Note over Discover: 开始设备发现流程
    
    Discover->>Discover: 检查是否已初始化
    alt 首次运行
        Discover->>Discover: 扫描库目录
        Note over Discover: 查找 ggml-* 库文件
        
        loop 每个发现的库
            Discover->>Runner: 启动引导Runner
            Runner->>GPU: 加载GPU库
            GPU->>Device: 枚举物理设备
            Device-->>GPU: 返回设备列表
            GPU-->>Runner: 设备信息
            Runner-->>Discover: HTTP响应设备数据
            Note over Discover: 设备验证和去重
        end
        
        Discover->>Discover: 过滤不兼容设备
        Discover->>Discover: 标记初始化完成
    else 已初始化
        Note over Discover: 使用缓存的设备信息
    end
    
    loop 活跃Runner更新
        Discover->>Runner: 查询设备状态
        Runner->>GPU: 获取实时VRAM信息
        GPU-->>Runner: 当前内存使用
        Runner-->>Discover: 更新设备信息
    end
    
    Discover-->>Main: 返回可用设备列表
```

## 🏗️ 设备信息结构

### 核心数据结构
```mermaid
classDiagram
    class DeviceInfo {
        +DeviceID: ml.DeviceID
        +Description: string
        +TotalMemory: uint64
        +FreeMemory: uint64
        +Library: string
        +Variant: string
        +Compute: string
        +DriverMajor: int
        +DriverMinor: int
        +Compare() DeviceComparison
    }
    
    class GpuInfo {
        +DeviceID: ml.DeviceID
        +Name: string
        +TotalMemory: uint64
        +FreeMemory: uint64
        +ComputeMajor: int
        +ComputeMinor: int
        +DriverMajor: int
        +DriverMinor: int
        +DependencyPath: []string
    }
    
    class DeviceID {
        +Library: string
        +ID: string
        +Variant: string
    }
    
    DeviceInfo --> DeviceID : 包含
    GpuInfo --> DeviceID : 包含
    
    note for DeviceInfo "ML后端设备信息"
    note for GpuInfo "发现模块GPU信息"
    note for DeviceID "设备唯一标识"
```

### 设备比较算法
```go
type DeviceComparison int

const (
    DevicesDifferent DeviceComparison = iota
    SameBackendDevice    // 相同后端相同设备
    DuplicateDevice      // 不同后端相同物理设备
)

func (d DeviceInfo) Compare(other DeviceInfo) DeviceComparison {
    // 1. 检查是否为完全相同的设备
    if d.Library == other.Library && d.DeviceID.ID == other.DeviceID.ID {
        return SameBackendDevice
    }
    
    // 2. 检查是否为相同物理设备的不同接口
    if d.isSamePhysicalDevice(other) {
        return DuplicateDevice  
    }
    
    return DevicesDifferent
}
```

## 🎯 GPU库管理

### 多GPU库架构
```mermaid
graph LR
    subgraph "GPU库目录结构"
        LibPath[OLLAMA_MODELS/lib/<br/>GPU库根目录]
        CUDALib[cuda_*/<br/>CUDA库版本]
        ROCmLib[rocm/<br/>ROCm库]
        VulkanLib[vulkan/<br/>Vulkan库]
        MetalLib[metal/<br/>Metal库]
    end
    
    subgraph "库文件组织"
        GGMLCUDA[ggml-cuda<br/>CUDA后端]
        GGMLROCm[ggml-rocm<br/>ROCm后端]
        GGMLVulkan[ggml-vulkan<br/>Vulkan后端]
        GGMLMetal[ggml-metal<br/>Metal后端]
    end
    
    subgraph "动态加载"
        DynLoad[动态库加载<br/>dlopen/LoadLibrary]
        SymResolve[符号解析<br/>函数指针获取]
        InitCheck[初始化检查<br/>兼容性验证]
    end
    
    LibPath --> CUDALib
    LibPath --> ROCmLib
    LibPath --> VulkanLib
    LibPath --> MetalLib
    
    CUDALib --> GGMLCUDA
    ROCmLib --> GGMLROCm
    VulkanLib --> GGMLVulkan
    MetalLib --> GGMLMetal
    
    GGMLCUDA --> DynLoad
    GGMLROCm --> SymResolve
    GGMLVulkan --> InitCheck
    GGMLMetal --> InitCheck
```

### 库优先级策略
```mermaid
flowchart TD
    Start[开始选择GPU库] --> CheckEnv{检查环境变量}
    
    CheckEnv -->|OLLAMA_LLM_LIBRARY指定| UseSpecific[使用指定库]
    CheckEnv -->|未指定| AutoDetect[自动检测]
    
    AutoDetect --> DetectHardware[检测硬件类型]
    
    DetectHardware --> NVIDIACheck{NVIDIA GPU?}
    NVIDIACheck -->|是| SelectCUDA[选择CUDA库]
    NVIDIACheck -->|否| AMDCheck{AMD GPU?}
    
    AMDCheck -->|是| SelectROCm[选择ROCm库]
    AMDCheck -->|否| AppleCheck{Apple Silicon?}
    
    AppleCheck -->|是| SelectMetal[选择Metal库]  
    AppleCheck -->|否| SelectVulkan[选择Vulkan库]
    
    SelectCUDA --> VersionCheck[版本兼容性检查]
    SelectROCm --> VersionCheck
    SelectMetal --> VersionCheck
    SelectVulkan --> VersionCheck
    UseSpecific --> VersionCheck
    
    VersionCheck --> Success[库选择成功]
```

## 💾 内存管理策略

### VRAM监控机制
```mermaid
sequenceDiagram
    participant Scheduler as 调度器
    participant Discover as 发现模块
    participant Runner as 活跃Runner
    participant GPU as GPU驱动
    
    loop 定期更新(每3秒)
        Scheduler->>Discover: 请求设备状态更新
        Discover->>Discover: 遍历活跃Runner列表
        
        loop 每个活跃Runner
            Discover->>Runner: HTTP GET /info
            Runner->>GPU: 查询当前VRAM使用
            GPU-->>Runner: 返回内存统计
            Runner-->>Discover: 设备信息JSON
            Discover->>Discover: 更新设备内存状态
        end
        
        Discover-->>Scheduler: 返回更新后的设备列表
        Scheduler->>Scheduler: 根据内存状态调整调度策略
    end
```

### 内存分配策略
```mermaid
graph TB
    subgraph "内存评估"
        ModelSize[模型大小评估<br/>权重 + KV缓存]
        MemReserve[内存预留<br/>系统缓冲区]
        FragAssess[碎片评估<br/>可用连续内存]
    end
    
    subgraph "分配决策"
        SingleGPU[单GPU分配<br/>内存充足时]
        MultiGPU[多GPU分配<br/>模型过大时]
        CPUOffload[CPU卸载<br/>GPU内存不足]
    end
    
    subgraph "优化策略"
        Quantization[动态量化<br/>FP16/INT8转换]
        LayerSplit[层级分割<br/>跨设备分布]
        StreamLoad[流式加载<br/>按需分配]
    end
    
    ModelSize --> SingleGPU
    MemReserve --> MultiGPU
    FragAssess --> CPUOffload
    
    SingleGPU --> Quantization
    MultiGPU --> LayerSplit  
    CPUOffload --> StreamLoad
```

## 🔧 设备过滤机制

### 兼容性检查流程
```mermaid
flowchart TD
    AllDevices[所有检测到的设备] --> DedupeCheck[去重检查]
    
    DedupeCheck --> SameDevice{相同物理设备?}
    SameDevice -->|是| PrioritySelect[优先级选择<br/>CUDA>ROCm>Vulkan]
    SameDevice -->|否| CompatCheck[兼容性检查]
    
    CompatCheck --> DriverCheck{驱动版本检查}
    DriverCheck -->|版本过低| FilterOut[过滤掉设备]
    DriverCheck -->|版本合适| ComputeCheck{计算能力检查}
    
    ComputeCheck -->|能力不足| FilterOut
    ComputeCheck -->|能力充足| MemoryCheck{内存检查}
    
    MemoryCheck -->|内存不足| FilterOut
    MemoryCheck -->|内存充足| StabilityTest[稳定性测试]
    
    StabilityTest --> TestLoad[加载测试运行器]
    TestLoad --> TestPass{测试通过?}
    TestPass -->|失败| FilterOut
    TestPass -->|成功| AddToList[加入可用设备列表]
    
    PrioritySelect --> AddToList
    FilterOut --> End[结束检查]
    AddToList --> End
```

### 设备优先级算法
```go
type DevicePriority struct {
    Device   GpuInfo
    Score    int
    Category string
}

func calculateDevicePriority(device GpuInfo) DevicePriority {
    score := 0
    category := "unknown"
    
    // 基础性能分数
    score += int(device.ComputeMajor) * 100
    score += int(device.ComputeMinor) * 10
    
    // 内存容量分数
    memGB := device.TotalMemory / (1024 * 1024 * 1024)
    score += int(memGB) * 50
    
    // 库类型加权
    switch device.Library {
    case "CUDA":
        score += 1000
        category = "nvidia"
    case "ROCm":
        score += 800  
        category = "amd"
    case "Metal":
        score += 600
        category = "apple"
    case "Vulkan":
        score += 400
        category = "universal"
    }
    
    // 新驱动加分
    if device.DriverMajor >= 12 { // 现代驱动
        score += 100
    }
    
    return DevicePriority{device, score, category}
}
```

## 📊 性能监控指标

### 关键监控指标
| 指标类别 | 指标名称 | 正常范围 | 异常阈值 |
|----------|----------|----------|----------|
| **发现延迟** | 设备发现时间 | < 5s | > 15s |
| **内存精度** | VRAM查询误差 | < 5% | > 20% |
| **设备稳定性** | 设备掉线率 | < 1% | > 5% |
| **更新频率** | 状态更新间隔 | 3s | > 10s |

### 故障诊断
```bash
# 检查设备发现日志
ollama logs | grep "discovering"

# 手动触发设备重新发现  
killall ollama
OLLAMA_DEBUG=1 ollama serve

# 检查特定GPU库
ldd /usr/local/lib/ollama/ggml-cuda.so  # Linux
otool -L /usr/local/lib/ollama/ggml-metal.so  # macOS
```

---

**核心代码文件**:
- `discover/gpu.go` - GPU设备发现主逻辑
- `discover/runner.go` - Runner进程通信
- `discover/types.go` - 设备信息数据结构
- `ml/backend/ggml/ggml.go` - GGML后端集成

**下一步**: 查看 [并发模型](./10-并发模型.md) 了解Goroutine和Channel的使用模式