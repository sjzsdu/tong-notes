# Flow360 模块依赖关系

## 包引用关系图

```mermaid
graph TB
    subgraph "External Dependencies"
        numpy[numpy 2.0+]
        scipy[scipy]
        pandas[pandas]
        matplotlib[matplotlib]
        paraview[paraview]
        vtk[vtk]
        gmsh[gmsh]
        pydantic[pydantic 2.8+]
        jsonschema[jsonschema]
        aiohttp[aiohttp]
        requests[requests]
        flow360[flow360]
    end
    
    subgraph "Core Modules"
        simcloud[simcloud/]
        flow360scripts[flow360scripts/]
        flow360validation[flow360validation/]
        uvf[uvf/]
    end
    
    subgraph "flow360scripts Submodules"
        pipeline[pipeline/]
        preprocess[preprocess/]
        postprocess[postprocess/]
        visualize[visualize/]
        utilities[utilities/]
    end
    
    subgraph "flow360validation Submodules"
        validate[validate/]
        schema[schema/]
        webservice[webservice/]
        editor[editor/]
        workbench[workbench/]
    end
    
    %% External dependencies
    flow360scripts --> numpy
    flow360scripts --> scipy
    flow360scripts --> pandas
    flow360scripts --> matplotlib
    flow360scripts --> pydantic
    flow360scripts --> jsonschema
    flow360scripts --> requests
    flow360scripts --> flow360
    
    visualize --> paraview
    visualize --> vtk
    preprocess --> gmsh
    postprocess --> aiohttp
    
    %% Internal dependencies
    flow360scripts --> simcloud
    flow360validation --> jsonschema
    pipeline --> utilities
    preprocess --> utilities
    postprocess --> utilities
    visualize --> utilities
    
    validate --> schema
    webservice --> validate
    editor --> validate
    workbench --> validate
    
    %% Cross-module dependencies
    flow360validation --> flow360scripts
    uvf --> utilities
```

该依赖关系图展现了Flow360项目的模块架构。核心模块simcloud提供基础设施，flow360scripts实现主要业务逻辑，flow360validation负责数据验证，各模块通过明确的依赖关系协同工作。

## 内部模块交互图

```mermaid
graph LR
    subgraph "Pipeline Layer"
        A1[casePipeline]
        A2[meshVisualizePipeline]
        A3[caseVisualizePipeline]
        A4[adaptiveMeshPipeline]
        A5[geometryConversionPipeline]
    end
    
    subgraph "Processing Layer"
        B1[preprocess/]
        B2[postprocess/]
        B3[visualize/]
    end
    
    subgraph "Validation Layer"
        C1[validateFlow360Case]
        C2[validateFlow360Mesh]
        C3[validateFlow360Geometry]
    end
    
    subgraph "Infrastructure Layer"
        D1[simcloud.pipeline.client]
        D2[simcloud.logging]
        D3[utilities/]
        D4[errorCodes]
    end
    
    subgraph "Data Layer"
        E1[JSON Schema Files]
        E2[Configuration Files]
        E3[Mesh Files]
        E4[Result Files]
    end
    
    %% Pipeline to Processing
    A1 --> B1
    A1 --> B2
    A2 --> B3
    A3 --> B3
    A4 --> B1
    A5 --> B1
    
    %% Processing to Validation
    B1 --> C1
    B1 --> C2
    B1 --> C3
    
    %% All layers use Infrastructure
    A1 --> D1
    A2 --> D1
    A3 --> D1
    B1 --> D3
    B2 --> D3
    B3 --> D3
    C1 --> D2
    C2 --> D2
    C3 --> D2
    
    %% Infrastructure accesses Data
    D1 --> E1
    D1 --> E2
    D3 --> E3
    B2 --> E4
```

内部模块交互采用分层架构：Pipeline层协调整体流程，Processing层执行具体处理逻辑，Validation层确保数据正确性，Infrastructure层提供通用服务，Data层管理各类文件和配置。

## 关键类依赖图

```mermaid
classDiagram
    class PipelineScript {
        +runCommand()
        +runCommandAsync()
        +uploadFiles()
        +downloadFiles()
        +updateDatabase()
    }
    
    class CaseType {
        <<enumeration>>
        NEW
        RESUME
        FORK
        +getCaseType()
    }
    
    class StopType {
        <<enumeration>>
        TIME
        ITERATION
        CONVERGENCE
    }
    
    class Logger {
        +support()
        +debug()
        +user()
        +error()
    }
    
    class Progress {
        +updateProgress()
        +setTotal()
        +increment()
    }
    
    class ValidationError {
        +errorCode
        +message
        +context
    }
    
    %% Core pipeline classes
    class MeshVisualizePipeline {
        +run()
        +findWallsFromMeshJson()
        +populateSliceIsoMetaJson()
    }
    
    class CasePipeline {
        +run()
        +validateCaseInputFiles()
        +runMeshPartitioner()
        +runFlow360Solver()
    }
    
    class GeometryConversionPipeline {
        +run()
        +convertGeometry()
        +validateGeometry()
    }
    
    %% Inheritance and composition
    PipelineScript <|-- MeshVisualizePipeline
    PipelineScript <|-- CasePipeline
    PipelineScript <|-- GeometryConversionPipeline
    
    CasePipeline --> CaseType : uses
    CasePipeline --> StopType : uses
    PipelineScript --> Logger : uses
    PipelineScript --> Progress : uses
    
    %% Validation dependencies
    ValidationError <-- MeshVisualizePipeline : may throw
    ValidationError <-- CasePipeline : may throw
    ValidationError <-- GeometryConversionPipeline : may throw
```

关键类设计体现了面向对象的设计原则：PipelineScript作为基类提供通用Pipeline功能，具体Pipeline类继承并实现特定逻辑，枚举类提供类型安全，工具类提供通用服务。

## 配置和数据流

```mermaid
flowchart LR
    subgraph "Configuration Sources"
        A1[setup.py]
        A2[JSON Schema Files]
        A3[Environment Variables]
        A4[Command Line Args]
    end
    
    subgraph "Configuration Processing"
        B1[ConfigLoader]
        B2[ArgumentParser]
        B3[JSONSchema Validator]
        B4[Environment Checker]
    end
    
    subgraph "Runtime Configuration"
        C1[Pipeline Config]
        C2[Validation Rules]
        C3[Deployment Mode]
        C4[Resource Paths]
    end
    
    subgraph "Execution Context"
        D1[PipelineScript Instance]
        D2[Logger Configuration]
        D3[Progress Tracking]
        D4[Error Handling]
    end
    
    A1 --> B1
    A2 --> B3
    A3 --> B4
    A4 --> B2
    
    B1 --> C1
    B2 --> C1
    B3 --> C2
    B4 --> C3
    
    C1 --> D1
    C2 --> D1
    C3 --> D2
    C4 --> D3
```

配置和数据流展示了从静态配置到运行时环境的转换过程。系统支持多种配置源，通过统一的处理机制转换为运行时配置，最终驱动Pipeline的执行。

## 依赖关系特点

1. **松耦合设计**：各模块通过明确的接口交互，降低耦合度
2. **分层架构**：Infrastructure层为上层提供稳定的基础服务
3. **插件化扩展**：新的Pipeline可以轻松集成到现有框架中
4. **配置驱动**：通过配置文件和环境变量控制系统行为
5. **错误隔离**：每个模块的错误不会影响其他模块的正常运行

这种设计保证了系统的可维护性、可扩展性和稳定性，支持大规模CFD计算任务的可靠执行。